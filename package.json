{
  "name": "expensemanager-api",
  "version": "1.0.0",
  "description": "Expense Manager backend (Express + MongoDB) with WebAuthn + JWT authentication",
  "private": true,
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@simplewebauthn/server": "^10.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "license": "UNLICENSED"
}
