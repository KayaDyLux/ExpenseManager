# ---- Backend (API) envs ----
# Replace <username>, <password>, <host>, <db>, <replicaSet> with real values in your private .env
MONGODB_URI="mongodb+srv://<username>:<password>@<host>/<db>?tls=true&authSource=admin&replicaSet=<replicaSet>"
MONGODB_DB=ExpenseManager
JWT_SECRET=change-me-in-prod

# CORS origin allowed for API calls
ORIGIN=http://localhost:5173

# Dev convenience
OWNER_EMAIL=swapna@swapnade.com
ENFORCE_ALLOWLIST=false
